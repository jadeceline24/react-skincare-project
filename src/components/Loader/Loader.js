import React from 'react'
import {LoaderWrapper, Logo} from './LoaderStyle'
const Loader = () => {
  return (
    <LoaderWrapper className='absolute'>
      <Logo>Hell</Logo>
      </LoaderWrapper>
  )
}

export default Loader